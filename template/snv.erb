

[
  rdf:type gvo:SNV ;
  faldo:location [
    a faldo:ExactPosition ;
    faldo:position <%= @npos %> ;
    faldo:reference <<%= reference_uri(@obj.chrom) %>> ;
  ];

  rdfs:seeAlso "<%= @obj.id %>" ;
  gvo:ref "<%= @nref %>" ;
  gvo:alt "<%= @nalt %>" ;
  gvo:qual "<%= @obj.qual %>" ;
  gvo:filter "<%= @obj.filter %>" ;
  # TODO
  gvo:info []

].
